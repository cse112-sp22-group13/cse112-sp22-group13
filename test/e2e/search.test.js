describe("Search Tests", () => {
    beforeEach(async () => {
        await page.goto("http://localhost:3000");
    });

    it("Search with African cuisine", async () => {
        
    });

    it("Search with invalid cuisine", async () => {

    });
});